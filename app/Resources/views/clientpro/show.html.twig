{% extends 'base.html.twig' %}

{% block body %}
    <h1>Clientpro</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ clientPro.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ clientPro.nom }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('clientpro_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('clientpro_edit', { 'id': clientPro.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
